# Setup Guide

1. Install Java 11 or higher.
2. Install SQL Server and make sure the target database exists.
3. Open this project in VS Code or IntelliJ.
4. Update `DbConfig.java` with your database settings.
5. Run `ConnectionTest.java` first.
6. If connection works, run `MainUI.java`.
