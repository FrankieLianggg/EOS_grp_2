# Design Patterns

## Abstract Factory Pattern
`DaoFactory` centralizes the creation of DAO objects.

Benefits:
- reduces coupling
- simplifies object creation
- makes the library easier to extend

## Separation of Concerns
Each package has one main responsibility, which makes the code easier to test and maintain.
